# 发货额度统计 · 服务器部署说明（quota_app，Linux/宝塔）

> 适用：将本目录 `quota_server/` 上传到服务器独立运行。
> 已按约定：服务器上不带本地缓存、首次由用户重新上传表C/表B/表E/表R；无需并发锁（1~2 人使用）。

## 1. 目录内容
```
quota_server/
├── quota_app.py            # Flask 应用（端口内部 5050）
├── quota_core.py           # 计算逻辑（表C筛选/三表回款/订舱总价）
├── quota_regions.yaml      # 大区配置（俄罗斯/美洲）
├── config.yaml             # 表C/B/E/R 文件名等（勿删）
├── requirements.txt
├── templates/quota_dashboard.html
└── 数据源/汇率对应txt.txt    # 默认汇率兜底
```

## 2. 上传与安装（宝塔 Linux）
1. 把整个 `quota_server/` 上传到站点目录，例如 `/www/wwwroot/quota.xxx.com/`，保持结构不变。
2. 服务器安装 Python 3.11，然后：
```bash
cd /www/wwwroot/quota.xxx.com
python3.11 -m pip install -r requirements.txt
```
3. 确认目录可写（运行时自动创建 `workspaces/quota/<大区>/`）：
```bash
chown -R www:www /www/wwwroot/quota.xxx.com
```

## 3. 启动（waitress）
```bash
cd /www/wwwroot/quota.xxx.com
nohup waitress-serve --host=127.0.0.1 --port=5050 --channel-timeout=600 --threads=8 quota_app:app > app.log 2>&1 &
```
- 必须加 `--channel-timeout=600`：计算 1~3 分钟不返回字节，默认 120s 会被切断。
- 若与主收款看板同服务器，主看板用 5000，本应用用 5050，互不冲突。

## 4. 反向代理（宝塔站点设置）
- 站点 → 反向代理 → 目标 URL：`http://127.0.0.1:5050`
- nginx 调大限制（防止大文件/长计算被切断）：
```nginx
client_max_body_size 200m;
proxy_read_timeout  600s;
proxy_send_timeout  600s;
```

## 5. 密码
- 默认上传密码：`888888`（见 `quota_app.py` 的 `UPLOAD_PASSWORD`）。
- 如需修改：改 `quota_app.py` 后重启 waitress，或按计划后续改为环境变量。

## 6. 首次使用（服务器上重新上传，不带缓存）
1. 打开页面 → 俄罗斯大区 → “上传数据并运行”。
2. 首次必须上传 4 个文件：表C(国贸合同标的台账 .xlsx) / 表B(外币回款台账 .xlsx) / 表E(gm_ht_hthkmx .xls) / 表R(人民币回款台账 .xlsx)；汇率可不传用默认。
3. 点“开始运行”，等待遮罩显示预计时间与秒数；完成后核对：发货可用额度应约 `3,371.81` 万元（以你上传的台账为准）。

## 7. 验收
- 上传 `俄罗斯订舱发货.xlsx` 计算总价：三种格式均应识别，总额 `21,686,200.00 元 = 2,168.62 万元`；
- 页面显示“发货可用额度 − 本次发货总价 = 余额”，余额应约 `1,203.19` 万元；
- 合同/梯号重叠会拒绝并提示；
- 运行期间刷新页面不再卡死（waitress 多线程）。

## 8. 日常更新
- 更新代码：覆盖 `quota_app.py`、`quota_core.py`、`templates/quota_dashboard.html` 后重启：
```bash
pkill -f waitress-serve
nohup waitress-serve --host=127.0.0.1 --port=5050 --channel-timeout=600 --threads=8 quota_app:app > app.log 2>&1 &
```
- 更新区域配置：覆盖 `quota_regions.yaml`、`config.yaml`。
- 定期备份：`workspaces/`（各区域上传数据与结果）和 `app.log`。